import { styled } from "styled-components";

const SkillsStyle = styled.section`
    display: flex;
    flex-direction: column;
    gap: 45px;
    /* padding: 60px 0; */
`;
const ContainerContentStyle = styled.div`
    display: flex;
    flex-direction: row;
    flex-wrap: wrap;
    gap: 16px;
`;

export {
    SkillsStyle, ContainerContentStyle
}